export enum routeNames {
    about = "/",
    profile = "/profile/:userId",
    myProfile = "/profile",
    dialogs = "/dialogs",
    users = "/users",
    login = "/login",
    register = "/register",
    chat = "/chat/:userId",
    chatlist = "/chatlist",
    notFound = "/*",
}